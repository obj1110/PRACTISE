//完全的BST 成为 CBT,当然最底下一排是可以有空
#include<iostream> 
#include<vector> 
#include<algorithm>

using namespace std;
const int maxn = 1010;
const int inf = 2050;
struct node{
	int data;
	node* left,*right;
};

int n,number[maxn],CBT[maxn],index = 0;

//首先进行建树，然后进行二叉树的层序遍历工作 
void insert(node* root,vector<int> & result) {
	
}

void Level(node* root){
	
}
void 

void init(){
	scanf("%d",&n);
	for(int i=0;i<n;i++){
		scanf("%d",&num[i]);
	}
}

int main(){
	
}

